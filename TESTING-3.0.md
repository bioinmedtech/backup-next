# 🧪 Инструкция по тестированию админ-панели v3.0

**Версия**: 3.0  
**Дата**: Декабрь 2024  
**Статус**: Готово к тестированию

---

## ✅ Быстрый старт (5 минут)

### Шаг 1: Перейти на админ-панель

```
https://new.bioinmed.ru/admin
```

**Должно произойти:**
- Редирект на `/admin/login` (если не авторизированы)
- Или показать админ-панель (если уже авторизированы)

### Шаг 2: Авторизация

**Учетные данные:**
```
Логин: admin
Пароль: ChangeMe123!
```

**Должно произойти:**
- Форма авторизации отображается красиво (стеклянный морфизм, анимации)
- После входа → редирект на `/admin`

### Шаг 3: Проверить главную страницу

**Должно быть видно:**
- ✅ Фиксированный header (градиент #2fbdef → #0077bd)
- ✅ Боковое меню слева (280px)
- ✅ 4 статистических карточки на Dashboard:
  - **14** услуг
  - **5** вопросов FAQ (из config.php)
  - **3** отзыва (из config.php)
  - **1** пользователь (admin)

---

## 📊 Тестирование каждого раздела

### 🔧 Раздел "Услуги"

**Путь:** Боковое меню → Услуги

**Проверить:**
1. ✅ Отображаются 8 форм для редактирования
2. ✅ Поля: ID, Название, Цена, Описание
3. ✅ Первые 2-3 поля заполнены данными из `/data/services.json`
4. ✅ Кнопка "Сохранить услуги" работает
5. ✅ После сохранения появляется зеленое сообщение "✓ Услуги сохранены"
6. ✅ Версия сохранена в `/data/versions/service_*.json`

**Проверить версионность:**
```bash
ls -la /var/www/bioinmed-next/data/versions/
# Должны появиться файлы:
# service_osteopathy.json
# service_massage.json
# и т.д.

cat /var/www/bioinmed-next/data/versions/service_osteopathy.json | head -20
# Должна быть видна структура с timestamp, user, data
```

---

### ❓ Раздел "FAQ"

**Путь:** Боковое меню → FAQ

**Проверить:**
1. ✅ Отображаются поля для вопросов и ответов
2. ✅ Загружаются существующие FAQ из конфига
3. ✅ Кнопка "Сохранить FAQ" работает
4. ✅ После сохранения → сообщение "✓ FAQ сохранены"
5. ✅ Данные сохраняются в `/data/faqs.json`

---

### ⭐ Раздел "Отзывы"

**Путь:** Боковое меню → Отзывы

**Проверить:**
1. ✅ Поля: Пациент, Оценка (1-5), Результат
2. ✅ Выпадающий список оценок работает
3. ✅ Кнопка "Сохранить отзывы" работает
4. ✅ Данные сохраняются в `/data/reviews.json`

---

### 👥 Раздел "Пользователи" (только для admin)

**Путь:** Боковое меню → Пользователи

**Проверить:**
1. ✅ Отображается форма "Создать пользователя"
2. ✅ Поля: Логин, Пароль, Роль (viewer/editor/admin)
3. ✅ Список существующих пользователей ниже
4. ✅ Для каждого пользователя есть форма редактирования
5. ✅ Может изменить роль, пароль, статус
6. ✅ Кнопка "Создать" добавляет нового пользователя

**Тест создания пользователя:**
- Логин: `test_editor`
- Пароль: `Test123!`
- Роль: `editor`
- Нажать "Создать"

**Должно произойти:**
- ✅ Зеленое сообщение "✓ Пользователь создан"
- ✅ Новый пользователь появляется в списке
- ✅ Данные сохранены в `/data/users.json`

**Проверить:** Попробовать войти как `test_editor`

---

## 🎨 Проверка дизайна

### Desktop (1024px+)

- ✅ Header фиксирован сверху
- ✅ Боковое меню слева (280px)
- ✅ Основной контент справа
- ✅ Нет горизонтального скролла
- ✅ Форма-поля красиво стилизованы

### Tablet (768px-1023px)

- ✅ Боковое меню меньше (200px)
- ✅ Контент адаптирован
- ✅ Кнопки достаточного размера

### Mobile (375px-767px)

- ✅ Боковое меню адаптировано или скрыто
- ✅ Контент на полную ширину
- ✅ Формы удобны на маленьком экране
- ✅ Кнопки достаточного размера для касания

---

## 🔄 Проверка редиректов

**URL `/cabinet` должен редиректить на `/admin`:**
```bash
curl -I https://new.bioinmed.ru/cabinet
# Ожидаем: HTTP/1.1 301 Moved Permanently
#         Location: https://new.bioinmed.ru/admin
```

**URL `/admin/login` должен показать форму входа:**
```bash
https://new.bioinmed.ru/admin/login
# Должно быть видно: Красивая форма авторизации
```

---

## 📋 Проверка удаления "скоро" блоков

### На главной странице

**Путь:** https://new.bioinmed.ru

**Проверить:**
1. ✅ Нет текста "Страница с подробностями скоро"
2. ✅ Нет текста "Скоро добавим детали"
3. ✅ Есть блок "Диагностика + первичный приём со скидкой 20%"
4. ✅ Есть кнопки "Позвонить" и "WhatsApp"

**Поиск "скоро":**
```bash
grep -r "скоро" /var/www/bioinmed-next/includes/components/Components.php
# Должно быть пусто (no matches)
```

---

## 🔒 Проверка ролей и доступа

### Тест 1: Admin может всё

**Вход:** `admin` / `ChangeMe123!`

- ✅ Видит Dashboard
- ✅ Может редактировать Услуги
- ✅ Может редактировать FAQ
- ✅ Может редактировать Отзывы
- ✅ Видит раздел "Пользователи"

### Тест 2: Editor может редактировать контент

**Вход:** `test_editor` / `Test123!` (созданный в предыдущем тесте)

- ✅ Видит Dashboard
- ✅ Видит Услуги, FAQ, Отзывы
- ✅ **НЕ видит** раздел "Пользователи"
- ✅ Может редактировать Услуги
- ✅ Может редактировать FAQ
- ✅ Может редактировать Отзывы

### Тест 3: Viewer только смотрит

**Создать пользователя с ролью `viewer`, проверить:**
- ✅ Видит Dashboard
- ✅ **НЕ видит** никаких разделов для редактирования

---

## 📱 Проверка на реальных устройствах

### iOS (iPhone/iPad)

```
https://new.bioinmed.ru/admin
```

- ✅ Сайт загружается за < 3 секунды
- ✅ Меню не прыгает при скролле
- ✅ Формы удобны (достаточные отступы, размеры кнопок)
- ✅ Нет горизонтального скролла

### Android

- ✅ Всё то же самое

---

## 🐛 Проверка на ошибки

### В браузере (DevTools → Console)

```javascript
// Не должно быть красных ошибок
// Должны быть только yellow warnings (если они есть)
```

### На сервере

```bash
tail -f /var/log/php8.3-fpm.log
# Проверить: нет ошибок PHP при использовании админ-панели
```

---

## 📊 Проверка файлов

### Структура `/data/`

```bash
ls -la /var/www/bioinmed-next/data/
# Должны быть:
# -rw-r--r-- services.json
# -rw-r--r-- faqs.json
# -rw-r--r-- reviews.json
# -rw-r--r-- users.json
# drwxr-xr-x versions/
```

### Содержимое `services.json`

```bash
cat /var/www/bioinmed-next/data/services.json | head -30
# Должны быть 14 услуг с полной информацией
```

### Содержимое `versions/`

```bash
ls -la /var/www/bioinmed-next/data/versions/
# После редактирования услуг должны появиться service_*.json файлы
```

---

## ✅ Финальный чек-лист

- [ ] Админ-панель загружается на https://new.bioinmed.ru/admin
- [ ] Авторизация работает с admin / ChangeMe123!
- [ ] Dashboard показывает правильную статистику
- [ ] Раздел "Услуги" показывает 14 услуг
- [ ] Сохранение услуг создает версии в `/data/versions/`
- [ ] FAQ редактируется и сохраняется
- [ ] Отзывы редактируются и сохраняются
- [ ] Раздел "Пользователи" доступен только admin
- [ ] Можно создать нового пользователя (editor/viewer)
- [ ] Роли работают корректно (editor не видит управление пользователями)
- [ ] Нет блоков "скоро" и "coming soon" на главной
- [ ] Есть блок "Диагностика + первичный приём со скидкой 20%"
- [ ] URL /cabinet редиректит на /admin (301)
- [ ] Мобильный дизайн работает корректно
- [ ] Нет красных ошибок в console браузера

---

## 🚀 После тестирования

Если всё прошло успешно:

1. ✅ Обновить `/QUICK-START.md` с инструкциями для клиента
2. ✅ Отправить скриншоты админ-панели
3. ✅ Отправить credentials (логин/пароль по защищенному каналу)
4. ✅ Провести обучение клиента (45-60 минут)

---

**Тестирование завершено успешно! ✨**

Для вопросов: обратитесь к документации в `/RELEASE-NOTES-3.0.md`
